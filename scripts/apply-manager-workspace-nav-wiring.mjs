#!/usr/bin/env node

console.log('Legacy manager workspace nav autofix is disabled. No source files were rewritten.');
console.log('Use explicit source edits plus npm run ui-source:guard for UI shell changes.');
