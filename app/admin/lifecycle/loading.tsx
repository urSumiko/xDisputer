import ConsoleInstantLoading from '../../../components/console/ConsoleInstantLoading';

export default function Loading() {
  return <ConsoleInstantLoading role="manager" title="Client lifecycle desk." description="Showing the manager lifecycle shell now while approval and handoff queues load." datasetLabel="Lifecycle queue" />;
}
