# Auth Feature Slice

Owns sign in, signup, session UI, and account entry flows.

Modernization targets:

- separate form UI from auth behavior
- add schema contracts after Zod is installed
- keep route/page files as orchestration only
- reuse shared primitives after the UI system is introduced
