# Templates Feature Slice

Owns template manifests, selection, classification, template metadata, and manager-owned template behavior.

Modernization targets:

- move template metadata contracts into schemas
- keep template runtime logic out of JSX
- isolate template selection UI from generation actions
- preserve DOCX/template domain behavior while improving boundaries
