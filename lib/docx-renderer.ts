import Docxtemplater from 'docxtemplater';
import PizZip from 'pizzip';
import { hardenGeneratedDocx } from './docx-safety';
import { assertHydrationContract } from './docx-hydration-contract';
import { buildAccountHydrationBlocks, buildInquiryHydrationBlocks } from './dispute-hydration-blocks';
import { createStructuralSnapshot, validateStructuralInvariance } from './docx-structural-guard';
import { applyDocxFlowRulesToBlob, applyLetterFlowRules } from './docx-flow';

export const DOCX_MIME = 'application/vnd.openxmlformats-officedocument.wordprocessingml.document';
const WORD_NS = 'http://schemas.openxmlformats.org/wordprocessingml/2006/main';
const XML_NS = 'http://www.w3.org/XML/1998/namespace';
const STATEMENT_PREFIX = ['Pursuant to ', '15 USC'].join('');
const IDENTITY_THEFT_DISPUTE_STATEMENT = 'Pursuant to 15 USC 1681a(3), this account does not constitute a legitimate consumer obligation. My personal information was used without authorization, and this tradeline is the direct result of identity theft.';
const DISPUTE_EXCLUDED_ADDRESS_FIELD = /^(?:PHONE(?:\s+NO\.?)?|TELEPHONE|MOBILE|EMAIL|E-?MAIL|COUNTRY|DOB|SSN)\s*:/i;
const ACCOUNT_SECTION_PATTERNS = [/^(?:(?:[IVXLCDM]+|\d+|[A-Z])\s*[.)]?\s*)?(?:DISPUTE(?:D)?\s+ACCOUNTS?(?:\s*(?:&|AND)\s*(?:INFORMATION|ITEMS))?|ACCOUNTS?\s+(?:IN\s+DISPUTE|TO\s+BE\s+DISPUTED)|(?:INACCURATE|UNVERIFIED|NEGATIVE)\s+ACCOUNTS?|FRAUDULENT\s+ACCOUNTS?(?:\s*\([^)]*\))?)(?:\s*[:\-–—].*)?$/i, /^.*\bDISPUTE(?:D)?\s+ACCOUNTS?\b.*$/i, /^FRAUDULENT\s+ACCOUNTS?\s+.*(?:DELETION|BLOCKING|RE-?ASSERTED).*$/i];
const HARD_INQUIRY_LABEL = /^(?:(?:[IVXLCDM]+|\d+|[A-Z])\s*[.)]?\s*)?HARD\s+(?:CREDIT\s+)?INQUIR(?:Y|IES)(?:\s*[:\-]\s*(.*))?$/i;
const DOWNSTREAM_SECTION_PATTERNS = [/^(?:(?:[IVXLCDM]+|\d+|[A-Z])\s*[.)]?\s*)?HARD\s+(?:CREDIT\s+)?INQUIR(?:Y|IES)(?:\s*[:\-–—].*)?$/i, /^(?:(?:[IVXLCDM]+|\d+|[A-Z])\s*[.)]?\s*)?FINAL\s+OPPORTUNITY\s+TO\s+COMPLY\b.*$/i, /^(?:(?:[IVXLCDM]+|\d+|[A-Z])\s*[.)]?\s*)?FAILURE\s+TO\s+COMPLY\b.*$/i, /^(?:(?:[IVXLCDM]+|\d+|[A-Z])\s*[.)]?\s*)?IF\s+YOU\s+FAIL\s+TO\s+COMPLY\b.*$/i, /^(?:(?:[IVXLCDM]+|\d+|[A-Z])\s*[.)]?\s*)?LEGAL\s+(?:BASIS|DEMAND)(?:\s+AND\s+NOTICE\s+OF\s+DUTY)?\b.*$/i, /^(?:(?:[IVXLCDM]+|\d+|[A-Z])\s*[.)]?\s*)?MANDATORY\s+DEMANDS?\b.*$/i, /^(?:(?:[IVXLCDM]+|\d+|[A-Z])\s*[.)]?\s*)?REQUEST(?:ED)?\s+ACTION\b.*$/i, /^(?:(?:[IVXLCDM]+|\d+|[A-Z])\s*[.)]?\s*)?NOTICE\s+OF\s+DUTY\b.*$/i, /^(?:(?:[IVXLCDM]+|\d+|[A-Z])\s*[.)]?\s*)?CONSEQUENCES?\s+OF\s+NON-?COMPLIANCE\b.*$/i, /^(?:(?:[IVXLCDM]+|\d+|[A-Z])\s*[.)]?\s*)?ENCLOSURES?\b.*$/i, /^(?:(?:[IVXLCDM]+|\d+|[A-Z])\s*[.)]?\s*)?CONCLUSION\b.*$/i, /^CLOSING$/i, /^GOVERN\s+YOURSELF\s+ACCORDINGLY\.?$/i];
const NEXT_SECTION_PATTERNS = DOWNSTREAM_SECTION_PATTERNS;
const LEGAL_BOUNDARY_PATTERNS = DOWNSTREAM_SECTION_PATTERNS;
const SIGNATURE_PATTERN = /^(?:Sincerely|Respectfully(?:\s+submitted)?|Best\s+regards|Regards|Yours\s+(?:truly|sincerely)|Very\s+truly\s+yours|Thank\s+you),?$/i;

export type TemplateValue = string | number | boolean | Array<Record<string, string>>;
export type PlaceholderValues = Record<string, TemplateValue>;
export type ReferenceDisputeValues = { consumerName: string; addressLines: string[]; dob: string; ssn: string; letterDate: string; bureauName: string; bureauAddressLines: string[]; disputeItems?: string[]; hardInquiryItems?: string[]; fraudItems?: string[] };

export async function renderDocxTemplate(template: File, values: PlaceholderValues): Promise<Blob> { const zip = new PizZip(await template.arrayBuffer()); const document = new Docxtemplater(zip, { delimiters: { start: '{{', end: '}}' }, paragraphLoop: true, linebreaks: true, nullGetter: () => '' }); document.render(values); const generated = document.getZip().generate({ type: 'blob', mimeType: DOCX_MIME, compression: 'DEFLATE' }); const output = await hardenGeneratedDocx(generated); return applyDocxFlowRulesToBlob(output); }
function paragraphs(body: Element) { return Array.from(body.children).filter((node) => node.namespaceURI === WORD_NS && node.localName === 'p') as Element[]; }
function content(paragraph: Element) { return Array.from(paragraph.getElementsByTagNameNS(WORD_NS, 't')).map((node) => node.textContent || '').join('').trim(); }
function findParagraph(all: Element[], patterns: RegExp[]) { return all.find((paragraph) => patterns.some((pattern) => pattern.test(content(paragraph)))); }
function styleOf(paragraph: Element) { const runs = Array.from(paragraph.children).filter((node) => node.namespaceURI === WORD_NS && node.localName === 'r'); return (runs.find((run) => content(run)) || runs[0] || paragraph.ownerDocument.createElementNS(WORD_NS, 'w:r')).cloneNode(true) as Element; }
function blankRun(source: Element) { const run = source.cloneNode(true) as Element; Array.from(run.children).forEach((node) => { if (!(node.namespaceURI === WORD_NS && node.localName === 'rPr')) run.removeChild(node); }); return run; }
function writeLines(paragraph: Element, lines: string[]) { const doc = paragraph.ownerDocument; const style = styleOf(paragraph); Array.from(paragraph.children).forEach((node) => { if (!(node.namespaceURI === WORD_NS && node.localName === 'pPr')) paragraph.removeChild(node); }); lines.forEach((line, index) => { if (index) { const run = blankRun(style); run.appendChild(doc.createElementNS(WORD_NS, 'w:br')); paragraph.appendChild(run); } const run = blankRun(style); const value = doc.createElementNS(WORD_NS, 'w:t'); if (/^\s|\s$/.test(line)) value.setAttributeNS(XML_NS, 'xml:space', 'preserve'); value.textContent = line; run.appendChild(value); paragraph.appendChild(run); }); }
function cloneWithText(source: Element, lines: string[]) { const paragraph = source.cloneNode(true) as Element; writeLines(paragraph, lines); return paragraph; }
function forceIdentityStatementColor(paragraph: Element) { if (!content(paragraph).includes(IDENTITY_THEFT_DISPUTE_STATEMENT)) return paragraph; const doc = paragraph.ownerDocument; Array.from(paragraph.getElementsByTagNameNS(WORD_NS, 'r')).forEach((run) => { let properties = Array.from(run.children).find((node) => node.namespaceURI === WORD_NS && node.localName === 'rPr') as Element | undefined; if (!properties) { properties = doc.createElementNS(WORD_NS, 'w:rPr'); run.insertBefore(properties, run.firstChild); } Array.from(properties.children).filter((node) => node.namespaceURI === WORD_NS && node.localName === 'color').forEach((node) => properties?.removeChild(node)); const color = doc.createElementNS(WORD_NS, 'w:color'); color.setAttributeNS(WORD_NS, 'w:val', 'FF0000'); properties.appendChild(color); }); return paragraph; }
function cloneStatementWithTemplateStyle(source: Element, lines: string[]) { return forceIdentityStatementColor(cloneWithText(source, lines)); }
function paragraphProperties(paragraph: Element) { const existing = Array.from(paragraph.children).find((node) => node.namespaceURI === WORD_NS && node.localName === 'pPr') as Element | undefined; if (existing) return existing; const properties = paragraph.ownerDocument.createElementNS(WORD_NS, 'w:pPr'); paragraph.insertBefore(properties, paragraph.firstChild); return properties; }
function ensureParagraphProperty(paragraph: Element, localName: string) { const properties = paragraphProperties(paragraph); const existing = Array.from(properties.children).find((node) => node.namespaceURI === WORD_NS && node.localName === localName) as Element | undefined; if (existing) return existing; const property = paragraph.ownerDocument.createElementNS(WORD_NS, `w:${localName}`); properties.appendChild(property); return property; }
function keepParagraphLinesTogether(paragraph: Element) { ensureParagraphProperty(paragraph, 'keepLines'); ensureParagraphProperty(paragraph, 'widowControl'); }
function keepWithNextParagraph(paragraph: Element) { keepParagraphLinesTogether(paragraph); ensureParagraphProperty(paragraph, 'keepNext'); }
function keepDisputeBlockTogether(paragraphs: Element[]) { paragraphs.filter(Boolean).forEach((paragraph, index) => { if (index < paragraphs.length - 1) keepWithNextParagraph(paragraph); else keepParagraphLinesTogether(paragraph); }); }
function preserveSsnDateBoundary(body: Element, ssnParagraph: Element, dateParagraph: Element) { const all = paragraphs(body); const ssnIndex = all.indexOf(ssnParagraph); const dateIndex = all.indexOf(dateParagraph); if (ssnIndex < 0 || dateIndex <= ssnIndex) return; keepParagraphLinesTogether(ssnParagraph); keepParagraphLinesTogether(dateParagraph); }
function findPopulatedHeaderParagraphs(body: Element, values: ReferenceDisputeValues) { const all = paragraphs(body); const ssnParagraph = all.find((paragraph) => /\bSSN\s*:/i.test(content(paragraph)) && content(paragraph).includes(values.ssn)); if (!ssnParagraph) return null; const index = all.indexOf(ssnParagraph); const dateParagraph = all.slice(index + 1).find((paragraph) => content(paragraph).includes(values.letterDate)); return dateParagraph ? { ssnParagraph, dateParagraph } : null; }
function removeHardInquiryLabels(body: Element) { paragraphs(body).filter((paragraph) => HARD_INQUIRY_LABEL.test(content(paragraph))).forEach((paragraph) => paragraph.parentNode?.removeChild(paragraph)); }
async function finalizeRenderedDisputeTemplate(blob: Blob, values: ReferenceDisputeValues) { assertHydrationContract(); const zip = new PizZip(await blob.arrayBuffer()); const file = zip.file('word/document.xml'); if (!file) return blob; const originalXml = file.asText(); const snapshot = createStructuralSnapshot(originalXml); const xml = new DOMParser().parseFromString(originalXml, 'application/xml'); const body = xml.getElementsByTagNameNS(WORD_NS, 'body')[0]; if (!body) return blob; const header = findPopulatedHeaderParagraphs(body, values); if (header) preserveSsnDateBoundary(body, header.ssnParagraph, header.dateParagraph); removeHardInquiryLabels(body); applyLetterFlowRules(body); paragraphs(body).forEach(forceIdentityStatementColor); const outputXml = new XMLSerializer().serializeToString(xml); validateStructuralInvariance(snapshot, outputXml); zip.file('word/document.xml', outputXml); return hardenGeneratedDocx(zip.generate({ type: 'blob', mimeType: DOCX_MIME, compression: 'DEFLATE' })); }
function resolved(values: ReferenceDisputeValues) { if (values.disputeItems || values.hardInquiryItems) return { accounts: values.disputeItems || [], inquiries: values.hardInquiryItems || [] }; const combined = values.fraudItems || []; return { accounts: combined.filter((entry) => /^(Account|Creditor)\s+Name\s*:/i.test(entry.trim())), inquiries: combined.filter((entry) => !/^(Account|Creditor)\s+Name\s*:/i.test(entry.trim())) }; }
function disputeAddressLines(values: ReferenceDisputeValues) { return values.addressLines.map((line) => line.trim()).filter(Boolean).filter((line) => !DISPUTE_EXCLUDED_ADDRESS_FIELD.test(line)); }
function accountValues(text: string) { const lines = text.split('\n').map((line) => line.trim()).filter(Boolean); const accountName = (lines.find((line) => /^(?:Account|Creditor)\s+Name\s*:/i.test(line)) || '').replace(/^(?:Account|Creditor)\s+Name\s*:\s*/i, ''); const accountNumber = (lines.find((line) => /^Account\s+Number\s*:/i.test(line)) || '').replace(/^Account\s+Number\s*:\s*/i, ''); return { account_name: accountName, account_number: accountNumber, account_line: [accountName, accountNumber].filter(Boolean).join(' - '), display_text: text, statement_line: IDENTITY_THEFT_DISPUTE_STATEMENT, legal_statement: IDENTITY_THEFT_DISPUTE_STATEMENT, dispute_statement: IDENTITY_THEFT_DISPUTE_STATEMENT }; }
function inquiryValues(text: string) { const clean = text.replace(/\s*[–—]\s*/g, ' — ').replace(/\s+/g, ' ').trim(); const match = clean.match(/^(.+?)\s+[—-]\s+(.+)$/); const inquiryName = match?.[1]?.trim() || clean; const inquiryDate = match?.[2]?.trim() || ''; return { inquiry_name: inquiryName, inquiry_date: inquiryDate, inquiry_line: clean, account_name: inquiryName, account_number: inquiryDate, account_line: clean, display_text: clean, statement_line: IDENTITY_THEFT_DISPUTE_STATEMENT, legal_statement: IDENTITY_THEFT_DISPUTE_STATEMENT, dispute_statement: IDENTITY_THEFT_DISPUTE_STATEMENT }; }
function disputePlaceholderValues(values: ReferenceDisputeValues): PlaceholderValues { const source = resolved(values); const address = disputeAddressLines(values); const accounts = source.accounts.map(accountValues); const inquiries = source.inquiries.map(inquiryValues); const combinedItems = [...accounts, ...inquiries]; return { consumer_name: values.consumerName, client_name: values.consumerName, name: values.consumerName, address: address.join('\n'), address_inline: address.join(' '), address_line_1: address[0] || '', address_line_2: address.slice(1).join(' '), dob: values.dob, ssn: values.ssn, ssn_masked: values.ssn, date: values.letterDate, letter_date: values.letterDate, document_date: values.letterDate, bureau_name: values.bureauName, bureau_address: values.bureauAddressLines.join('\n'), bureau_address_line_1: values.bureauAddressLines[0] || '', bureau_address_line_2: values.bureauAddressLines.slice(1).join(' '), accounts: combinedItems, dispute_accounts: combinedItems, hard_inquiries: [], account_lines: combinedItems.map((item) => [item.display_text, item.statement_line].join('\n')).join('\n\n'), hard_inquiry_lines: '' }; }
function terminalBodyBoundary(body: Element) { return Array.from(body.children).find((node) => node.namespaceURI === WORD_NS && node.localName === 'sectPr') || null; }
function insertMappedDisputeItems(body: Element, source: { accounts: string[]; inquiries: string[] }, bureauName: string) { if (!source.accounts.length && !source.inquiries.length) throw new Error('No matching account or inquiry records were found.'); const accountBlocks = buildAccountHydrationBlocks({ bureau: bureauName, accounts: source.accounts, statement: IDENTITY_THEFT_DISPUTE_STATEMENT }); const inquiryBlocks = buildInquiryHydrationBlocks({ bureau: bureauName, inquiries: source.inquiries, statement: IDENTITY_THEFT_DISPUTE_STATEMENT }); const all = paragraphs(body); const accountHeading = findParagraph(all, ACCOUNT_SECTION_PATTERNS); const hardInquiryHeading = findParagraph(all, [HARD_INQUIRY_LABEL]); const legalBoundary = findParagraph(all, LEGAL_BOUNDARY_PATTERNS); const signatureBoundary = all.find((paragraph) => SIGNATURE_PATTERN.test(content(paragraph))); const terminalBoundary = terminalBodyBoundary(body); const fallbackBoundary = legalBoundary || signatureBoundary || terminalBoundary; if (accountBlocks.length && !accountHeading) throw new Error('Disputed accounts anchor was not found in the latest DOCX template.'); function indexOf(paragraph: Element | null | undefined) { return paragraph ? all.indexOf(paragraph) : -1; } function firstBoundaryAfter(start: Element | null | undefined, fallback: Node | null | undefined) { const startIndex = indexOf(start); if (startIndex < 0) return fallback || null; return all.slice(startIndex + 1).find((paragraph) => NEXT_SECTION_PATTERNS.some((pattern) => pattern.test(content(paragraph))) || LEGAL_BOUNDARY_PATTERNS.some((pattern) => pattern.test(content(paragraph))) || SIGNATURE_PATTERN.test(content(paragraph))) || fallback || null; } function regionBetween(start: Element | null | undefined, boundary: Node | null | undefined) { if (!start || !boundary || !(boundary instanceof Element)) return []; const startIndex = all.indexOf(start); const boundaryIndex = all.indexOf(boundary); return startIndex >= 0 && boundaryIndex > startIndex ? all.slice(startIndex + 1, boundaryIndex) : []; } function styleFrom(region: Element[], heading: Element | null | undefined, fallback: Node | null | undefined) { return region.find((paragraph) => content(paragraph) && !content(paragraph).startsWith(STATEMENT_PREFIX) && !HARD_INQUIRY_LABEL.test(content(paragraph))) || heading || (fallback instanceof Element ? fallback : undefined) || all.find((paragraph) => content(paragraph))!; } function statementStyleFrom(region: Element[], fallback: Element) { return region.find((paragraph) => content(paragraph).startsWith(STATEMENT_PREFIX)) || fallback; } function spacerFrom(region: Element[]) { return region.find((paragraph) => !content(paragraph)); } function insertBefore(boundary: Node | null | undefined, node: Node) { return boundary ? body.insertBefore(node, boundary) : body.appendChild(node); } function addSpacer(boundary: Node | null | undefined, sourceSpacer: Element | undefined) { if (sourceSpacer) insertBefore(boundary, sourceSpacer.cloneNode(true)); } const accountBoundary = accountHeading ? firstBoundaryAfter(accountHeading, hardInquiryHeading || fallbackBoundary) : fallbackBoundary; const accountRegion = regionBetween(accountHeading, accountBoundary); const accountStyle = styleFrom(accountRegion, accountHeading, accountBoundary); const accountStatementStyle = statementStyleFrom(accountRegion, accountStyle); const accountSpacer = spacerFrom(accountRegion); accountRegion.forEach((paragraph) => paragraph.parentNode?.removeChild(paragraph)); addSpacer(accountBoundary, accountSpacer); accountBlocks.forEach((block) => { const accountParagraph = cloneWithText(accountStyle, block.lines); const statementParagraph = cloneStatementWithTemplateStyle(accountStatementStyle, [block.statement]); keepDisputeBlockTogether([accountParagraph, statementParagraph]); insertBefore(accountBoundary, accountParagraph); insertBefore(accountBoundary, statementParagraph); addSpacer(accountBoundary, accountSpacer); }); if (!inquiryBlocks.length) return; const inquiryBoundary = hardInquiryHeading ? firstBoundaryAfter(hardInquiryHeading, fallbackBoundary) : accountBoundary; const inquiryRegion = hardInquiryHeading ? [hardInquiryHeading, ...regionBetween(hardInquiryHeading, inquiryBoundary)] : []; const inquiryStyle = styleFrom(inquiryRegion, accountStyle, inquiryBoundary); const inquiryStatementStyle = statementStyleFrom(inquiryRegion, accountStatementStyle); const inquirySpacer = spacerFrom(inquiryRegion) || accountSpacer; inquiryRegion.forEach((paragraph) => paragraph.parentNode?.removeChild(paragraph)); addSpacer(inquiryBoundary, inquirySpacer); inquiryBlocks.forEach((block) => { const inquiryParagraph = cloneWithText(inquiryStyle, block.lines); const statementParagraph = cloneStatementWithTemplateStyle(inquiryStatementStyle, [block.statement]); keepDisputeBlockTogether([inquiryParagraph, statementParagraph]); insertBefore(inquiryBoundary, inquiryParagraph); insertBefore(inquiryBoundary, statementParagraph); addSpacer(inquiryBoundary, inquirySpacer); }); }
export async function renderReferenceDisputeDocx(reference: File, values: ReferenceDisputeValues): Promise<Blob> { assertHydrationContract(); const zip = new PizZip(await reference.arrayBuffer()); const file = zip.file('word/document.xml'); if (!file) throw new Error('DOCX document XML is unavailable.'); const documentXml = file.asText(); if (/\{\{\s*[#\/^]?[\w.-]+\s*\}\}/.test(documentXml)) return finalizeRenderedDisputeTemplate(await renderDocxTemplate(reference, disputePlaceholderValues(values)), values); const snapshot = createStructuralSnapshot(documentXml); const xml = new DOMParser().parseFromString(documentXml, 'application/xml'); if (xml.getElementsByTagName('parsererror').length) throw new Error('DOCX content could not be read.'); const body = xml.getElementsByTagNameNS(WORD_NS, 'body')[0]; if (!body) throw new Error('DOCX body is unavailable.'); const source = resolved(values); const all = paragraphs(body); const nonEmpty = all.filter((paragraph) => content(paragraph)); if (nonEmpty.length < 3) throw new Error('Reference header layout is incomplete.'); writeLines(nonEmpty[0], [values.consumerName, ...disputeAddressLines(values), `DOB: ${values.dob}`, `SSN: ${values.ssn}`]); writeLines(nonEmpty[1], [values.letterDate]); writeLines(nonEmpty[2], [values.bureauName, ...values.bureauAddressLines]); preserveSsnDateBoundary(body, nonEmpty[0], nonEmpty[1]); insertMappedDisputeItems(body, source, values.bureauName); const renderedParagraphs = paragraphs(body); const close = renderedParagraphs.find((paragraph) => SIGNATURE_PATTERN.test(content(paragraph))); if (close) { const signature = renderedParagraphs.slice(renderedParagraphs.indexOf(close) + 1).find((paragraph) => content(paragraph)); if (signature) writeLines(signature, [values.consumerName]); } applyLetterFlowRules(body); paragraphs(body).forEach(forceIdentityStatementColor); const outputXml = new XMLSerializer().serializeToString(xml); validateStructuralInvariance(snapshot, outputXml); zip.file('word/document.xml', outputXml); return hardenGeneratedDocx(zip.generate({ type: 'blob', mimeType: DOCX_MIME, compression: 'DEFLATE' })); }
export function isDocx(filename: string) { return /\.docx$/i.test(filename); }
